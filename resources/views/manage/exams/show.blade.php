@extends('layouts.app')
@section('title', 'Mock exam details · Manage')
@section('content')<nav aria-label="Breadcrumb"><ol class="breadcrumb"><li class="breadcrumb-item"><a href="{{ route('manage.exams.index') }}">Mock exams</a></li><li class="breadcrumb-item active" aria-current="page">Details</li></ol></nav><div class="d-flex flex-column flex-md-row justify-content-between gap-3 mb-4"><div><p class="text-primary fw-semibold mb-1">Owner view</p><h1 class="h2 mb-1">{{ $exam->title }}</h1><p class="text-body-secondary mb-0">{{ str_replace('_', ' ', ucfirst($exam->format_label)) }} · {{ $exam->status }}</p></div><div class="d-flex gap-2"><a class="btn btn-outline-primary" href="{{ route('manage.exams.edit', $exam) }}">Edit</a><a class="btn btn-outline-secondary" href="{{ route('manage.exams.preview', $exam) }}">Learner preview</a></div></div><div class="card mb-4"><div class="card-body"><dl class="row mb-0"><dt class="col-sm-3">Overall time</dt><dd class="col-sm-9">{{ $exam->time_limit_seconds ? $exam->time_limit_seconds.' seconds' : 'Untimed' }}</dd><dt class="col-sm-3">Description</dt><dd class="col-sm-9 preserve-lines">{{ $exam->description ?: 'None' }}</dd><dt class="col-sm-3">Instructions</dt><dd class="col-sm-9 preserve-lines">{{ $exam->instructions ?: 'None' }}</dd></dl></div></div><div class="vstack gap-3">@foreach ($exam->sections as $section)<section class="card"><div class="card-body"><div class="d-flex justify-content-between gap-3"><h2 class="h5">{{ $section->position + 1 }}. {{ $section->title }}</h2><span class="badge text-bg-light">{{ ucfirst($section->skill) }}</span></div>@if ($section->instructions)<p class="text-body-secondary preserve-lines">{{ $section->instructions }}</p>@endif<div class="vstack gap-2 mt-3">@foreach ($section->items as $item)<article class="border rounded p-3"><div class="d-flex justify-content-between gap-3"><strong>{{ $item->position + 1 }}. {{ $item->question?->prompt }}</strong><span class="badge text-bg-light">{{ $item->points }} points</span></div><p class="small text-body-secondary mb-2">Question #{{ $item->question_id }} · {{ $item->question?->status }}</p><ol class="mb-0">@foreach ($item->question?->options ?? [] as $option)<li class="{{ $option->is_correct ? 'text-success fw-semibold' : '' }}">{{ $option->option_key }}. {{ $option->content }} @if ($option->is_correct)(correct)@endif</li>@endforeach</ol></article>@endforeach</div></div></section>@endforeach</div>@endsection
