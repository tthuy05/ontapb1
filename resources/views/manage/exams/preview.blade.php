@extends('layouts.app')
@section('title', 'Mock exam preview · Manage')
@section('content')<nav aria-label="Breadcrumb"><ol class="breadcrumb"><li class="breadcrumb-item"><a href="{{ route('manage.exams.index') }}">Mock exams</a></li><li class="breadcrumb-item active" aria-current="page">Preview</li></ol></nav><div class="d-flex justify-content-between gap-3 mb-4"><div><p class="text-primary fw-semibold mb-1">Learner preview</p><h1 class="h2 mb-1">{{ $exam->title }}</h1><p class="text-body-secondary mb-0">{{ str_replace('_', ' ', ucfirst($exam->format_label)) }} · {{ $exam->status }}</p></div><a class="btn btn-outline-primary" href="{{ route('manage.exams.edit', $exam) }}">Edit</a></div><div class="alert alert-info">Preview only. Correct answers and explanations are intentionally hidden.</div>@if ($exam->description)<p class="lead text-body-secondary preserve-lines">{{ $exam->description }}</p>@endif<div class="vstack gap-4">@foreach ($exam->sections as $section)<section class="card"><div class="card-body p-4"><div class="d-flex justify-content-between gap-3"><h2 class="h5">{{ $section->position + 1 }}. {{ $section->title }}</h2><span class="badge text-bg-light">{{ ucfirst($section->skill) }}</span></div>@if ($section->instructions)<p class="text-body-secondary preserve-lines">{{ $section->instructions }}</p>@endif@foreach ($section->items as $item)<article class="border rounded p-3 mt-3"><h3 class="h6">Question {{ $item->position + 1 }}</h3><p class="lead">{{ $item->question?->prompt }}</p><ol class="mb-0">@foreach ($item->question?->options ?? [] as $option)<li>{{ $option->option_key }}. {{ $option->content }}</li>@endforeach</ol></article>@endforeach</div></section>@endforeach</div>@endsection
