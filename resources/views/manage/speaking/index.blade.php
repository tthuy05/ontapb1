@extends('layouts.app')

@section('title', 'Manage speaking')

@section('content')
    <div class="d-flex flex-column flex-md-row justify-content-between gap-3 mb-4"><div><p class="text-primary fw-semibold mb-1">Content management</p><h1 class="h2 mb-1">Speaking prompts</h1><p class="text-body-secondary mb-0">Timed practice prompts with browser-local recording and self-review metadata.</p></div><a class="btn btn-primary align-self-start" href="{{ route('manage.speaking.create') }}">Create prompt</a></div>
    <form class="card card-body mb-4" method="GET"><div class="row g-2 align-items-end"><div class="col-12 col-md-5"><label class="form-label" for="search">Search</label><input class="form-control" id="search" name="search" value="{{ request('search') }}"></div><div class="col-6 col-md-3"><label class="form-label" for="status">Status</label><select class="form-select" id="status" name="status"><option value="">All</option>@foreach (App\Models\SpeakingPrompt::STATUSES as $value)<option value="{{ $value }}" @selected(request('status') === $value)>{{ ucfirst($value) }}</option>@endforeach</select></div><div class="col-6 col-md-3"><label class="form-label" for="part_type">Part</label><select class="form-select" id="part_type" name="part_type"><option value="">All</option>@foreach (App\Models\SpeakingPrompt::PART_TYPES as $value)<option value="{{ $value }}" @selected(request('part_type') === $value)>{{ str_replace('_', ' ', ucfirst($value)) }}</option>@endforeach</select></div><div class="col-12 col-md-1"><button class="btn btn-outline-primary w-100" type="submit">Go</button></div></div></form>
    <div class="table-responsive"><table class="table align-middle"><thead><tr><th>Part</th><th>Title</th><th>Status</th><th>Submissions</th><th></th></tr></thead><tbody>@forelse ($prompts as $prompt)<tr><td>{{ str_replace('_', ' ', ucfirst($prompt->part_type)) }}</td><td><a href="{{ route('manage.speaking.show', $prompt) }}">{{ $prompt->title }}</a></td><td>{{ ucfirst($prompt->status) }}</td><td>{{ $prompt->submissions_count }}</td><td class="table-actions"><a class="btn btn-sm btn-outline-primary" href="{{ route('manage.speaking.edit', $prompt) }}">Edit</a> <a class="btn btn-sm btn-outline-secondary" href="{{ route('manage.speaking.preview', $prompt) }}">Preview</a></td></tr>@empty<tr><td colspan="5" class="text-body-secondary">No speaking prompts yet.</td></tr>@endforelse</tbody></table></div><div>{{ $prompts->links() }}</div>
@endsection
