@extends('layouts.app')
@section('title', $speakingPrompt->title.' · Manage Speaking')
@section('content')<div class="d-flex flex-column flex-md-row justify-content-between gap-3 mb-4"><div><p class="text-primary fw-semibold mb-1">{{ str_replace('_', ' ', ucfirst($speakingPrompt->part_type)) }} · {{ $speakingPrompt->status }}</p><h1 class="h2">{{ $speakingPrompt->title }}</h1><p class="text-body-secondary mb-0">{{ $speakingPrompt->submissions_count }} submission(s)</p></div><div class="d-flex gap-2"><a class="btn btn-outline-primary" href="{{ route('manage.speaking.edit', $speakingPrompt) }}">Edit</a><a class="btn btn-outline-secondary" href="{{ route('manage.speaking.preview', $speakingPrompt) }}">Preview</a></div></div><article class="card mb-4"><div class="card-body p-4"><h2 class="h5">Instructions</h2><div class="preserve-lines">{{ $speakingPrompt->instructions }}</div><p class="small text-body-secondary mt-3 mb-0">Preparation: {{ $speakingPrompt->preparation_seconds ?? 0 }} seconds · Speaking: {{ $speakingPrompt->speaking_seconds ?? 'Flexible' }} seconds</p></div></article><form method="POST" action="{{ route('manage.speaking.status.update', $speakingPrompt) }}" class="d-inline">@csrf @method('PATCH')<input type="hidden" name="status" value="{{ $speakingPrompt->status === 'active' ? 'inactive' : 'active' }}"><button class="btn btn-outline-warning" type="submit">{{ $speakingPrompt->status === 'active' ? 'Deactivate' : 'Activate' }}</button></form>@if ($submissions->isNotEmpty())<article class="card mt-4"><div class="card-body p-4"><h2 class="h5">Recent self-reviews</h2><ul class="mb-0">@foreach ($submissions as $submission)<li>{{ $submission->completed_at?->format('Y-m-d H:i') }} · {{ $submission->duration_seconds }} seconds · {{ $submission->notes ? 'notes saved' : 'no notes' }}</li>@endforeach</ul></div></article>@endif@endsection
