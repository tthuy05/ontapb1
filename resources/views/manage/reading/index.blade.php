@extends('layouts.app')
@section('title', 'Manage Reading · B1 English Self-Study')
@section('content')<div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center gap-3 mb-4"><div><p class="text-primary fw-semibold mb-1">Manage</p><h1 class="h2 mb-0">Reading passages</h1></div><a class="btn btn-primary" href="{{ route('manage.reading.create') }}">Create passage</a></div>
<form class="card card-body mb-4" method="GET" action="{{ route('manage.reading.index') }}"><div class="row g-3 align-items-end"><div class="col-12 col-lg-4"><label class="form-label" for="search">Search</label><input class="form-control" id="search" name="search" value="{{ request('search') }}"></div><div class="col-6 col-lg-3"><label class="form-label" for="topic">Topic</label><select class="form-select" id="topic" name="topic"><option value="">All topics</option>@foreach ($topics as $topic)<option value="{{ $topic->id }}" @selected((string) request('topic') === (string) $topic->id)>{{ $topic->name }}</option>@endforeach</select></div><div class="col-6 col-lg-2"><label class="form-label" for="status">Status</label><select class="form-select" id="status" name="status"><option value="">All</option>@foreach (\App\Models\Passage::STATUSES as $status)<option value="{{ $status }}" @selected(request('status') === $status)>{{ ucfirst($status) }}</option>@endforeach</select></div><div class="col-8 col-lg-2"><label class="form-label" for="source_type">Source</label><select class="form-select" id="source_type" name="source_type"><option value="">All</option>@foreach (\App\Models\Passage::SOURCE_TYPES as $sourceType)<option value="{{ $sourceType }}" @selected(request('source_type') === $sourceType)>{{ str_replace('_', ' ', ucfirst($sourceType)) }}</option>@endforeach</select></div><div class="col-4 col-lg-1 d-grid"><button class="btn btn-outline-primary" type="submit">Filter</button></div></div></form>
@if ($passages->isEmpty())<div class="card card-body text-center py-5"><h2 class="h5">No passages found</h2><p class="text-body-secondary">Create a small original passage and review it before activation.</p><a class="btn btn-primary align-self-center" href="{{ route('manage.reading.create') }}">Create passage</a></div>@else<div class="card"><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead><tr><th>Passage</th><th>Topic</th><th>Words</th><th>Questions</th><th>Source</th><th>Status</th><th><span class="visually-hidden">Actions</span></th></tr></thead><tbody>@foreach ($passages as $passage)<tr><td><strong>{{ $passage->title }}</strong></td><td>{{ $passage->topic?->name ?? 'Unassigned' }}</td><td>{{ $passage->word_count }}</td><td>{{ $passage->questions_count }}</td><td>{{ str_replace('_', ' ', ucfirst($passage->source_type)) }}</td><td>@include('partials.status-badge', ['status' => $passage->status])</td><td class="text-end table-actions"><div class="d-flex flex-wrap justify-content-end gap-2"><a class="btn btn-sm btn-outline-secondary" href="{{ route('manage.reading.show', $passage) }}">Preview</a><a class="btn btn-sm btn-outline-primary" href="{{ route('manage.reading.edit', $passage) }}">Edit</a><form method="POST" action="{{ route('manage.reading.status.update', $passage) }}">@csrf @method('PATCH')<input type="hidden" name="status" value="{{ $passage->status === 'active' ? 'inactive' : 'active' }}"><button class="btn btn-sm btn-outline-secondary" type="submit">{{ $passage->status === 'active' ? 'Deactivate' : 'Activate' }}</button></form></div></td></tr>@endforeach</tbody></table></div></div><div class="mt-4">{{ $passages->links() }}</div>@endif
@endsection
