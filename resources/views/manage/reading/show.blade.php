@extends('layouts.app')
@section('title', $passage->title.' · Reading preview')
@section('content')<nav aria-label="Breadcrumb"><ol class="breadcrumb"><li class="breadcrumb-item"><a href="{{ route('manage.reading.index') }}">Manage Reading</a></li><li class="breadcrumb-item active" aria-current="page">Preview</li></ol></nav><div class="d-flex justify-content-between gap-3 mb-4"><div><p class="text-primary fw-semibold mb-1">Owner preview</p><h1 class="h2 mb-1">{{ $passage->title }}</h1><p class="text-body-secondary mb-0">{{ $passage->word_count }} words · {{ $passage->status }}</p></div><a class="btn btn-outline-primary" href="{{ route('manage.reading.edit', $passage) }}">Edit</a></div><article class="card mb-4"><div class="card-body p-4 p-lg-5 preserve-lines fs-5">{{ $passage->body }}</div></article><h2 class="h4">Linked questions and answer review</h2>@forelse ($passage->questions as $question)<article class="card mb-3"><div class="card-body"><h3 class="h5">{{ $loop->iteration }}. {{ $question->prompt }}</h3><ul class="mb-0">@foreach ($question->options as $option)<li class="{{ $option->is_correct ? 'fw-semibold text-success' : '' }}">{{ $option->option_key }}. {{ $option->content }} @if ($option->is_correct)<span>(correct)</span>@endif</li>@endforeach</ul></div></article>@empty<p class="text-body-secondary">No linked questions.</p>@endforelse@endsection
