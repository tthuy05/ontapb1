@extends('layouts.app')
@section('title', 'Exercise details · Manage')
@section('content')<nav aria-label="Breadcrumb"><ol class="breadcrumb"><li class="breadcrumb-item"><a href="{{ route('manage.exercises.index') }}">Exercises</a></li><li class="breadcrumb-item active" aria-current="page">Details</li></ol></nav><div class="d-flex flex-column flex-md-row justify-content-between gap-3 mb-4"><div><p class="text-primary fw-semibold mb-1">Owner view</p><h1 class="h2 mb-1">{{ $exercise->title }}</h1><p class="text-body-secondary mb-0">{{ ucfirst($exercise->skill) }} · Difficulty {{ $exercise->difficulty }} · {{ $exercise->status }}</p></div><div class="d-flex gap-2"><a class="btn btn-outline-primary" href="{{ route('manage.exercises.edit', $exercise) }}">Edit</a><a class="btn btn-outline-secondary" href="{{ route('manage.exercises.preview', $exercise) }}">Learner preview</a></div></div><div class="card mb-4"><div class="card-body"><dl class="row mb-0"><dt class="col-sm-3">Topic</dt><dd class="col-sm-9">{{ $exercise->topic?->name ?? 'No topic' }}</dd><dt class="col-sm-3">Time limit</dt><dd class="col-sm-9">{{ $exercise->time_limit_seconds ? $exercise->time_limit_seconds.' seconds' : 'Untimed' }}</dd><dt class="col-sm-3">Instructions</dt><dd class="col-sm-9 preserve-lines">{{ $exercise->instructions ?: 'None' }}</dd></dl></div></div><div class="vstack gap-3">@foreach ($exercise->exerciseQuestions as $item)<article class="card"><div class="card-body"><div class="d-flex justify-content-between gap-3"><h2 class="h5">{{ $item->position + 1 }}. {{ $item->question?->prompt }}</h2><span class="badge text-bg-light">{{ $item->points }} points</span></div><p class="small text-body-secondary">Question #{{ $item->question_id }} · {{ $item->question?->status }}</p><ol>@foreach ($item->question?->options ?? [] as $option)<li class="{{ $option->is_correct ? 'text-success fw-semibold' : '' }}">{{ $option->option_key }}. {{ $option->content }} @if ($option->is_correct)(correct)@endif</li>@endforeach</ol><p class="preserve-lines mb-0"><strong>Explanation:</strong> {{ $item->question?->explanation ?: 'None' }}</p></div></article>@endforeach</div>@endsection
