@extends('layouts.app')
@section('title', 'Exercise preview · Manage')
@section('content')<nav aria-label="Breadcrumb"><ol class="breadcrumb"><li class="breadcrumb-item"><a href="{{ route('manage.exercises.index') }}">Exercises</a></li><li class="breadcrumb-item active" aria-current="page">Preview</li></ol></nav><div class="d-flex justify-content-between gap-3 mb-4"><div><p class="text-primary fw-semibold mb-1">Learner preview</p><h1 class="h2 mb-1">{{ $exercise->title }}</h1><p class="text-body-secondary mb-0">{{ ucfirst($exercise->skill) }} · {{ $exercise->status }}</p></div><a class="btn btn-outline-primary" href="{{ route('manage.exercises.edit', $exercise) }}">Edit</a></div><div class="alert alert-info">This is a preview only. Correct answers and explanations are intentionally hidden here.</div>@if ($exercise->instructions)<p class="lead preserve-lines">{{ $exercise->instructions }}</p>@endif<div class="vstack gap-4">@foreach ($exercise->exerciseQuestions as $item)<article class="card"><div class="card-body p-4"><h2 class="h5">Question {{ $item->position + 1 }}</h2>@if ($item->question?->passage)<div class="example-panel rounded p-3 mb-3"><h3 class="h6">{{ $item->question->passage->title }}</h3><p class="preserve-lines mb-0">{{ $item->question->passage->body }}</p></div>@elseif ($item->question?->listeningContent)<div class="example-panel rounded p-3 mb-3"><h3 class="h6">{{ $item->question->listeningContent->title }}</h3><p class="mb-0">Listening content</p></div>@endif<p class="lead">{{ $item->question?->prompt }}</p><ol>@foreach ($item->question?->options ?? [] as $option)<li>{{ $option->option_key }}. {{ $option->content }}</li>@endforeach</ol></div></article>@endforeach</div>@endsection
