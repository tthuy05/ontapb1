@extends('layouts.app')
@section('title', 'Manage questions · B1 English Self-Study')
@section('content')<div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center gap-3 mb-4"><div><p class="text-primary fw-semibold mb-1">Manage</p><h1 class="h2 mb-0">Objective questions</h1></div><a class="btn btn-primary" href="{{ route('manage.questions.create') }}">Create question</a></div>
<form class="card card-body mb-4" method="GET" action="{{ route('manage.questions.index') }}"><div class="row g-3 align-items-end"><div class="col-12 col-lg-4"><label class="form-label" for="search">Search</label><input class="form-control" id="search" name="search" value="{{ request('search') }}"></div><div class="col-6 col-lg-2"><label class="form-label" for="skill">Skill</label><select class="form-select" id="skill" name="skill"><option value="">All</option>@foreach (\App\Models\Question::SKILLS as $skill)<option value="{{ $skill }}" @selected(request('skill') === $skill)>{{ ucfirst($skill) }}</option>@endforeach</select></div><div class="col-6 col-lg-2"><label class="form-label" for="type">Type</label><select class="form-select" id="type" name="type"><option value="">All</option>@foreach (\App\Models\Question::TYPES as $type)<option value="{{ $type }}" @selected(request('type') === $type)>{{ str_replace('_', ' ', ucfirst($type)) }}</option>@endforeach</select></div><div class="col-6 col-lg-2"><label class="form-label" for="status">Status</label><select class="form-select" id="status" name="status"><option value="">All</option>@foreach (\App\Models\Question::STATUSES as $status)<option value="{{ $status }}" @selected(request('status') === $status)>{{ ucfirst($status) }}</option>@endforeach</select></div><div class="col-6 col-lg-2 d-grid"><button class="btn btn-outline-primary" type="submit">Filter</button></div></div></form>
@if ($questions->isEmpty())<div class="card card-body text-center py-5"><h2 class="h5">No questions found</h2><p class="text-body-secondary">Create a question, review its answer, then activate it.</p><a class="btn btn-primary align-self-center" href="{{ route('manage.questions.create') }}">Create question</a></div>@else<div class="card"><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead><tr><th>Question</th><th>Skill/type</th><th>Context</th><th>Options</th><th>Source</th><th>Status</th><th><span class="visually-hidden">Actions</span></th></tr></thead><tbody>@foreach ($questions as $question)<tr><td><strong>{{ \Illuminate\Support\Str::limit($question->prompt, 80) }}</strong><span class="d-block small text-body-secondary">#{{ $question->id }}</span></td><td>{{ ucfirst($question->skill) }} · {{ str_replace('_', ' ', $question->type) }}</td><td>{{ $question->passage?->title ?? $question->listeningContent?->title ?? 'Standalone' }}</td><td>{{ $question->options_count }}</td><td>{{ str_replace('_', ' ', ucfirst($question->source_type)) }}</td><td>@include('partials.status-badge', ['status' => $question->status])</td><td class="text-end table-actions"><div class="d-flex flex-wrap justify-content-end gap-2"><a class="btn btn-sm btn-outline-secondary" href="{{ route('manage.questions.show', $question) }}">Preview</a><a class="btn btn-sm btn-outline-primary" href="{{ route('manage.questions.edit', $question) }}">Edit</a><form method="POST" action="{{ route('manage.questions.status.update', $question) }}">@csrf @method('PATCH')<input type="hidden" name="status" value="{{ $question->status === 'active' ? 'inactive' : 'active' }}"><button class="btn btn-sm btn-outline-secondary" type="submit">{{ $question->status === 'active' ? 'Deactivate' : 'Activate' }}</button></form></div></td></tr>@endforeach</tbody></table></div></div><div class="mt-4">{{ $questions->links() }}</div>@endif
@endsection
