@extends('layouts.app')
@section('title', 'Question preview · Manage')
@section('content')<nav aria-label="Breadcrumb"><ol class="breadcrumb"><li class="breadcrumb-item"><a href="{{ route('manage.questions.index') }}">Questions</a></li><li class="breadcrumb-item active" aria-current="page">Preview</li></ol></nav><div class="d-flex justify-content-between gap-3 mb-4"><div><p class="text-primary fw-semibold mb-1">Owner preview</p><h1 class="h2 mb-1">Question #{{ $question->id }}</h1><p class="text-body-secondary mb-0">{{ ucfirst($question->skill) }} · {{ str_replace('_', ' ', $question->type) }} · {{ $question->status }}</p></div><a class="btn btn-outline-primary" href="{{ route('manage.questions.edit', $question) }}">Edit</a></div><article class="card mb-4"><div class="card-body"><h2 class="h4">{{ $question->prompt }}</h2><ol class="mb-4">@foreach ($question->options as $option)<li class="{{ $option->is_correct ? 'fw-semibold text-success' : '' }}">{{ $option->option_key }}. {{ $option->content }} @if ($option->is_correct)<span>(correct)</span>@endif</li>@endforeach</ol><h3 class="h6">Explanation</h3><p class="preserve-lines mb-0">{{ $question->explanation ?: 'No explanation yet.' }}</p></div></article><div class="card"><div class="card-body"><h2 class="h5">Context and provenance</h2><p class="mb-1">{{ $question->passage?->title ?? $question->listeningContent?->title ?? 'Standalone question' }}</p><p class="small text-body-secondary mb-0">{{ ucfirst($question->source_type) }}@if ($question->source_reference) · {{ $question->source_reference }}@endif</p></div></div>@endsection
