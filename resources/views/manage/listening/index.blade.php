@extends('layouts.app')
@section('title', 'Manage Listening · B1 English Self-Study')
@section('content')<div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center gap-3 mb-4"><div><p class="text-primary fw-semibold mb-1">Manage</p><h1 class="h2 mb-0">Listening content</h1></div><a class="btn btn-primary" href="{{ route('manage.listening.create') }}">Create Listening item</a></div>
<form class="card card-body mb-4" method="GET" action="{{ route('manage.listening.index') }}"><div class="row g-3 align-items-end"><div class="col-12 col-lg-4"><label class="form-label" for="search">Search</label><input class="form-control" id="search" name="search" value="{{ request('search') }}"></div><div class="col-6 col-lg-3"><label class="form-label" for="topic">Topic</label><select class="form-select" id="topic" name="topic"><option value="">All topics</option>@foreach ($topics as $topic)<option value="{{ $topic->id }}" @selected((string) request('topic') === (string) $topic->id)>{{ $topic->name }}</option>@endforeach</select></div><div class="col-6 col-lg-2"><label class="form-label" for="status">Status</label><select class="form-select" id="status" name="status"><option value="">All</option>@foreach (\App\Models\ListeningContent::STATUSES as $status)<option value="{{ $status }}" @selected(request('status') === $status)>{{ ucfirst($status) }}</option>@endforeach</select></div><div class="col-8 col-lg-2"><label class="form-label" for="source_type">Source</label><select class="form-select" id="source_type" name="source_type"><option value="">All</option>@foreach (\App\Models\ListeningContent::SOURCE_TYPES as $sourceType)<option value="{{ $sourceType }}" @selected(request('source_type') === $sourceType)>{{ str_replace('_', ' ', ucfirst($sourceType)) }}</option>@endforeach</select></div><div class="col-4 col-lg-1 d-grid"><button class="btn btn-outline-primary" type="submit">Filter</button></div></div></form>
@if ($contents->isEmpty())<div class="card card-body text-center py-5"><h2 class="h5">No Listening items found</h2><p class="text-body-secondary">Create a small original script and measured audio reference.</p><a class="btn btn-primary align-self-center" href="{{ route('manage.listening.create') }}">Create Listening item</a></div>@else<div class="card"><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead><tr><th>Title</th><th>Topic</th><th>Audio</th><th>Questions</th><th>Source</th><th>Status</th><th><span class="visually-hidden">Actions</span></th></tr></thead><tbody>@foreach ($contents as $content)<tr><td><strong>{{ $content->title }}</strong><span class="d-block small text-body-secondary">{{ $content->audio_path }}</span></td><td>{{ $content->topic?->name ?? 'Unassigned' }}</td><td>{{ $content->audio_size_bytes }} bytes</td><td>{{ $content->questions_count }}</td><td>{{ str_replace('_', ' ', ucfirst($content->source_type)) }}</td><td>@include('partials.status-badge', ['status' => $content->status])</td><td class="text-end table-actions"><div class="d-flex flex-wrap justify-content-end gap-2"><a class="btn btn-sm btn-outline-secondary" href="{{ route('manage.listening.show', $content) }}">Preview</a><a class="btn btn-sm btn-outline-primary" href="{{ route('manage.listening.edit', $content) }}">Edit</a><form method="POST" action="{{ route('manage.listening.status.update', $content) }}">@csrf @method('PATCH')<input type="hidden" name="status" value="{{ $content->status === 'active' ? 'inactive' : 'active' }}"><button class="btn btn-sm btn-outline-secondary" type="submit">{{ $content->status === 'active' ? 'Deactivate' : 'Activate' }}</button></form></div></td></tr>@endforeach</tbody></table></div></div><div class="mt-4">{{ $contents->links() }}</div>@endif
@endsection
