@extends('layouts.app')
@section('title', $writingPrompt->title.' · Manage Writing')
@section('content')<div class="d-flex flex-column flex-md-row justify-content-between gap-3 mb-4"><div><p class="text-primary fw-semibold mb-1">{{ str_replace('_', ' ', ucfirst($writingPrompt->task_type)) }} · {{ $writingPrompt->status }}</p><h1 class="h2">{{ $writingPrompt->title }}</h1><p class="text-body-secondary mb-0">{{ $writingPrompt->submissions_count }} submission(s)</p></div><div class="d-flex gap-2"><a class="btn btn-outline-primary" href="{{ route('manage.writing.edit', $writingPrompt) }}">Edit</a><a class="btn btn-outline-secondary" href="{{ route('manage.writing.preview', $writingPrompt) }}">Preview</a></div></div><article class="card mb-4"><div class="card-body p-4"><h2 class="h5">Instructions</h2><div class="preserve-lines">{{ $writingPrompt->instructions }}</div>@if($writingPrompt->guidance)<h2 class="h5 mt-4">Guidance</h2><div class="preserve-lines">{{ $writingPrompt->guidance }}</div>@endif</div></article><form method="POST" action="{{ route('manage.writing.status.update', $writingPrompt) }}" class="d-inline">@csrf @method('PATCH')<input type="hidden" name="status" value="{{ $writingPrompt->status === 'active' ? 'inactive' : 'active' }}"><button class="btn btn-outline-warning" type="submit">{{ $writingPrompt->status === 'active' ? 'Deactivate' : 'Activate' }}</button></form>@endsection
